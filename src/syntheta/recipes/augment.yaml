config_version: "0.1"
task: augment
method: seed_dataset
n: 5000

evolve:
  enabled: true
  rounds: 1
  strategies: [deepen, concretize, rephrase]

responses:
  enabled: true
  use_cot: false

filters:
  safety:
    enabled: true
  quality:
    enabled: true
    min_score: 0.7
